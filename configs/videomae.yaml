model:
  type: videomae
  num_class: null  # Auto-detected from dataset
  model_name: "OpenGVLab/VideoMAEv2-Base"
  freeze_backbone: false
  dropout: 0.1
  hidden_dim: null

training:
  batch_size: 8
  num_epochs: 50
  learning_rate: 0.0001
  optimizer: adamw
  weight_decay: 0.01
  scheduler: warmup_cosine
  scheduler_params: {}
  clip_grad_norm: 1.0
  warmup_steps: null
  warmup_epochs: 5
  label_smoothing: 0.1
  dropout: null
  eval_every: 1
  save_every: 5
  save_best: true
  early_stopping_patience: 10
  use_amp: true

data:
  raw_path: "DATA/Segmented"
  metadata: "DATA/Segmented"
  metadata_val: null
  skeleton_path: null
  dataset_type: videomae
  train_val_split: 0.8
  random_split: true
  num_frames: 16
  target_size: [224, 224]
  use_imagenet_norm: false
  normalize: true
  skeleton_layout: null
  graph_strategy: null
  augmentations: null
  processor_name: "MCG-NJU/videomae-base"
  fps: 30.0

device: cuda
seed: 42
output_dir: "./logs/videomae"
experiment_name: "videomae_experiment"